#include <bits/stdc++.h>
using namespace std;

// ( i == j && weight[i] <= maxWeight) ? D[i][j] = piece[i] : D[i][j] = 0
// ( i > j) ? D[i][j] = 0;
// () 

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int test;
	cin >> test;
	while( test--) {
		

	}
	return 0;
}

