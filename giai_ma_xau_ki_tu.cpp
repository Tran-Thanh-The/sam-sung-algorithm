#include <bits/stdc++.h>
using namespace std;

void solve( string s, int i) {
	for ( int i = 0; i < s.length(); i++) {
		
	}
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int test;
	cin >> test;
	while( test--) {
		string s;
		cin >> s;
		
		cout << solve(i, 0) << endl;
	}
	return 0;
}

